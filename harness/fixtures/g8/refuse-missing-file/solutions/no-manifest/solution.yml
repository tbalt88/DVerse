# This fixture deliberately has no rootcomponents.yml alongside this file.
# solution.yml itself is not read by G8; it exists only so the solution
# directory is non-empty and tracked by git.
SolutionUniqueName: nomanifestsolution
