namespace Fixture.Library;

/// <summary>
/// Trivial placeholder type so the fixture compiles into a real assembly.
/// This project has no tests of its own by design; its (hypothetical)
/// tests would live in a sibling test project, never nested here.
/// </summary>
public sealed class Placeholder
{
    public static string Greeting() => "hello";
}
