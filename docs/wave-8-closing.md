# Wave 8 closing: the flip and the wrap

Seat-side work closed 2026-08-14. The wave itself completes when the owner runs the actions only the owner can run; they are listed at the bottom and the repo is ready for them.

## Seat-side delivered

| Slice | What | Evidence |
|---|---|---|
| 8.2 | README refreshed to waves 0-7 reality: eleven gates, G12 row and honest-SKIP ladder snippet, 253 tests, diff refusal pair linked | commit `c791b75` |
| 8.3 | Enforced-vs-written-rule split verified HONEST by the independent L3 auditor reading the implementing code behind every label, not by seat self-attestation | `docs/assurance/l3-round-1.md` F9/F10 |
| L3 round 1 | External assurance armed and CONVERGED per the standing ruling: every load-bearing claim independently reproduced; one MAJOR limits gap found and fixed (G7 gates no PR pre-merge, now stated plainly); public-readiness sweep clean, zero leaks beyond deliberate disclosures | `docs/assurance/l3-round-1.md` |

## Engagement wrap (waves 0-8, one day short of three weeks)

What the engagement proved, in one line each:
- Declarative artifacts CAN carry mechanical governance: eleven gates refuse what the platform accepts silently, offline, with receipts.
- The refusal thesis holds at three altitudes: static (G4's inverted relationship), runtime (the plugin blocking bad input in the live app), and change-time (G12's diff refusal pair).
- An agent loop with spec-before-spawn, boot-from-lessons, and seat-only tenant writes shipped 253 tests, five golden imports, two live apps, a transferable skill (blind eval 20/20), and a semantic diff engine, with zero forbidden-file violations across every executor spawned.
- The lessons file compounded: 17 burned lessons became gate logic, spec rulings, and ultimately the evolved skill's content.

Process ratchets landed this engagement: platform-mirror as mandatory first-registration procedure; --activate-plugins plus post-import probes as standing import discipline; eval answer keys held out of tree; design-first slices for identity-bearing engineering; L3 auditor rotation from round 2.

Open backlog, carried honestly: G5 (unblocked, not built), O12 (edit-cycle id stability), O13 (multi-column forms), O14 (canvas rename experiment), Matter Canvas solution-awareness (deviates from Microsoft ALM best practice; remediation path documented in session record), G7 pre-merge coverage (would need a PR-safe online design).

## Owner actions to complete the wave (the go-live checklist)

1. **8.1 Scrub**: run the public-repo scrub checklist from the vault (Mac-only). The L3 sweep found nothing, but the checklist is the ruling authority.
2. **8.4 Flip**: switch repository visibility to public (owner-run by standing decision).
3. Tell the seat the flip is done, which triggers **8.5**: the archived seed repo gains its live forward link (seat executes with owner approval, brief unarchive-edit-rearchive).
4. **8.6 Series content**: via /dex-twin, out of this seat's scope.

Nothing else blocks. The badges go live the moment the repo does.

## Addendum (2026-08-14, post-audit, owner-approved): Matter Canvas made solution-aware

Microsoft's ALM guidance says canvas apps belong in solutions; Matter Canvas was Studio-born outside one. With the owner's approval this was closed after the L3 round, disclosed here as an addendum rather than silently folded into the audited state:

1. Added to DVerseCore via the maker portal (seat write); the platform registered it in Dataverse as `dv_mattercanvas_791bc` (our publisher prefix applied) with canvasappid d47a037c-9a54-4c47-ac27-a0da3f96dc41.
2. Platform-mirrored the packaging shape (clone, decompile): the YAML source format wants `canvasapps/<name>.meta.yml` plus the msapp EXPLODED as a directory (`<name>_DocumentUri/`) that pack re-zips, plus flat background-image and identity parts. The exploded msapp puts the app's internals under version control.
3. G8 refused the platform's real shape: its wave-1 CanvasApp template guessed a directory named after the schemaName; corrected to the decompile-confirmed `.meta.yml` file template (lesson 16's class: when a gate refuses platform-authored output, suspect the gate). Fixture updated; suite 253/253.
4. Golden import #6 (0.6.0.0) with `--activate-plugins`: org confirms the version, the canvas app as a solution component, the plugin still blocking bad input (NOPE-1 refused), the step still active.

The pre-flip audit (`docs/assurance/l3-round-1.md`) ran against the five-import state; this addendum changes the tree after that audit. The change is CI-verified and its claims carry their own receipts above; the audit's verdict (nothing blocks the flip) is not weakened by an addition that strengthens the round-trip claim, but the sequencing is stated here so the record stays honest.

Two representations of one app now ship deliberately: the `.pa.yaml` mirror (for gating and review, G11/G12) and the solution packaging (for import). The `docs/receipts/` inventory and README import table carry the sixth row.
